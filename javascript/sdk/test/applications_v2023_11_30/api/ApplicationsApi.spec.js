/**
 * Selling Partner API for Application Management
 * The Selling Partner API for Application Management lets you programmatically update the client secret on registered applications.
 *
 * The version of the OpenAPI document: 2023-11-30
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import sinon from 'sinon';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'applications_v2023_11_30', 'index.js');
const SellingPartnerApiForApplicationManagement = await import(modulePath);

let instance;
let sandbox;
const testEndpoint = 'https://localhost:3000';
const testAccessToken = "testAccessToken";

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = SellingPartnerApiForApplicationManagement[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}

// Generate mock requests and responses for each operation
const mockrotateApplicationClientSecretData = {
  request: {
  },
  response: {
    statusCode: 204,
    headers: {}
  }
};

describe('ApplicationsApi', () => {
  beforeEach(() => {
    sandbox = sinon.createSandbox();
    const apiClientInstance = new SellingPartnerApiForApplicationManagement.ApiClient(testEndpoint);
    apiClientInstance.applyXAmzAccessTokenToRequest(testAccessToken);
    sandbox.stub(apiClientInstance, 'callApi');
    instance = new SellingPartnerApiForApplicationManagement.ApplicationsApi(apiClientInstance);
  });

  afterEach(() => {
    sandbox.restore();
  });

  describe('rotateApplicationClientSecret', () => {
    it('should successfully call rotateApplicationClientSecret', async () => {
      instance.apiClient.callApi.resolves(mockrotateApplicationClientSecretData.response);

      const data = await instance.rotateApplicationClientSecret();

      expect(data).to.be.undefined;
    });

    it('should successfully call rotateApplicationClientSecretWithHttpInfo', async () => {
      instance.apiClient.callApi.resolves(mockrotateApplicationClientSecretData.response);

      const response = await instance.rotateApplicationClientSecretWithHttpInfo();

      expect(response).to.have.property('statusCode');
      expect(response.statusCode).to.equal(mockrotateApplicationClientSecretData.response.statusCode)
      expect(response).to.have.property('headers');
    });

    it('should handle API errors', async () => {
      const errorResponse = {
        errors: new Error('Expected error to be thrown'),
        statusCode: 400,
        headers: {}
      };
      instance.apiClient.callApi.rejects(errorResponse);

      try {
        await instance.rotateApplicationClientSecret();
        throw new Error('Expected error to be thrown');
      } catch (error) {
        expect(error).to.exist;
        expect(error.statusCode).to.equal(400);
      }
    });
  });

  describe('constructor', () => {
    it('should use default ApiClient when none provided', () => {
      const defaultInstance = new SellingPartnerApiForApplicationManagement.ApplicationsApi();
      expect(defaultInstance.apiClient).to.equal(SellingPartnerApiForApplicationManagement.ApiClient.instance);
    });

    it('should use provided ApiClient', () => {
      const customClient = new SellingPartnerApiForApplicationManagement.ApiClient();
      const customInstance = new SellingPartnerApiForApplicationManagement.ApplicationsApi(customClient);
      expect(customInstance.apiClient).to.equal(customClient);
    });
  });
});
