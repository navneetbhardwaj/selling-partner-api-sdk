/**
 * Selling Partner API for Retail Procurement Payments
 * The Selling Partner API for Retail Procurement Payments provides programmatic access to vendors payments data.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'vendorinvoices_v1', 'index.js');
const SellingPartnerApiForRetailProcurementPayments = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new SellingPartnerApiForRetailProcurementPayments.PartyIdentification();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(SellingPartnerApiForRetailProcurementPayments.PartyIdentification.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('PartyIdentification', () => {
  it('should create an instance of PartyIdentification', () => {
    expect(instance).to.be.a(SellingPartnerApiForRetailProcurementPayments.PartyIdentification);
  });

  it('should have the property partyId', () => {
    // verify property exists
    expect(instance).to.have.property('partyId');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.partyId = expectedValue;
    expect(instance.partyId).to.equal(expectedValue);
  });

  it('should have the property address', () => {
    // verify property exists
    expect(instance).to.have.property('address');

    // set and verify value
    const expectedValue = generateMockData('Address');
    instance.address = expectedValue;
    expect(instance.address).to.equal(expectedValue);
  });

  it('should have the property taxRegistrationDetails', () => {
    // verify property exists
    expect(instance).to.have.property('taxRegistrationDetails');

    // set and verify value
    const expectedValue = generateMockData('TaxRegistrationDetails', true);
    instance.taxRegistrationDetails = expectedValue;
    expect(instance.taxRegistrationDetails).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = SellingPartnerApiForRetailProcurementPayments[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
