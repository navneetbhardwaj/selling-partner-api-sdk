/**
 * Selling Partner API for Retail Procurement Orders
 * The Selling Partner API for Retail Procurement Orders provides programmatic access to vendor orders data.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'vendororders_v1', 'index.js');
const SellingPartnerApiForRetailProcurementOrders = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new SellingPartnerApiForRetailProcurementOrders.ImportDetails();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(SellingPartnerApiForRetailProcurementOrders.ImportDetails.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('ImportDetails', () => {
  it('should create an instance of ImportDetails', () => {
    expect(instance).to.be.a(SellingPartnerApiForRetailProcurementOrders.ImportDetails);
  });

  it('should have the property methodOfPayment', () => {
    // verify property exists
    expect(instance).to.have.property('methodOfPayment');

    // set and verify value
    const expectedValue = ['PaidByBuyer', 'CollectOnDelivery', 'DefinedByBuyerAndSeller', 'FOBPortOfCall', 'PrepaidBySeller', 'PaidBySeller', ][0];
    instance.methodOfPayment = expectedValue;
    expect(instance.methodOfPayment).to.equal(expectedValue);
  });

  it('should have the property internationalCommercialTerms', () => {
    // verify property exists
    expect(instance).to.have.property('internationalCommercialTerms');

    // set and verify value
    const expectedValue = ['ExWorks', 'FreeCarrier', 'FreeOnBoard', 'FreeAlongSideShip', 'CarriagePaidTo', 'CostAndFreight', 'CarriageAndInsurancePaidTo', 'CostInsuranceAndFreight', 'DeliveredAtTerminal', 'DeliveredAtPlace', 'DeliverDutyPaid', ][0];
    instance.internationalCommercialTerms = expectedValue;
    expect(instance.internationalCommercialTerms).to.equal(expectedValue);
  });

  it('should have the property portOfDelivery', () => {
    // verify property exists
    expect(instance).to.have.property('portOfDelivery');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.portOfDelivery = expectedValue;
    expect(instance.portOfDelivery).to.equal(expectedValue);
  });

  it('should have the property importContainers', () => {
    // verify property exists
    expect(instance).to.have.property('importContainers');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.importContainers = expectedValue;
    expect(instance.importContainers).to.equal(expectedValue);
  });

  it('should have the property shippingInstructions', () => {
    // verify property exists
    expect(instance).to.have.property('shippingInstructions');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.shippingInstructions = expectedValue;
    expect(instance.shippingInstructions).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = SellingPartnerApiForRetailProcurementOrders[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
