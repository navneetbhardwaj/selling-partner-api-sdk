/**
 * Selling Partner API for Pricing
 * The Selling Partner API for Pricing helps you programmatically retrieve product pricing and offer information for Amazon Marketplace products.
 *
 * The version of the OpenAPI document: v0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'pricing_v0', 'index.js');
const SellingPartnerApiForPricing = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new SellingPartnerApiForPricing.DetailedShippingTimeType();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(SellingPartnerApiForPricing.DetailedShippingTimeType.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('DetailedShippingTimeType', () => {
  it('should create an instance of DetailedShippingTimeType', () => {
    expect(instance).to.be.a(SellingPartnerApiForPricing.DetailedShippingTimeType);
  });

  it('should have the property minimumHours', () => {
    // verify property exists
    expect(instance).to.have.property('minimumHours');

    // set and verify value
    const expectedValue = generateMockData('Number');
    instance.minimumHours = expectedValue;
    expect(instance.minimumHours).to.equal(expectedValue);
  });

  it('should have the property maximumHours', () => {
    // verify property exists
    expect(instance).to.have.property('maximumHours');

    // set and verify value
    const expectedValue = generateMockData('Number');
    instance.maximumHours = expectedValue;
    expect(instance.maximumHours).to.equal(expectedValue);
  });

  it('should have the property availableDate', () => {
    // verify property exists
    expect(instance).to.have.property('availableDate');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.availableDate = expectedValue;
    expect(instance.availableDate).to.equal(expectedValue);
  });

  it('should have the property availabilityType', () => {
    // verify property exists
    expect(instance).to.have.property('availabilityType');

    // set and verify value
    const expectedValue = ['NOW', 'FUTURE_WITHOUT_DATE', 'FUTURE_WITH_DATE', ][0];
    instance.availabilityType = expectedValue;
    expect(instance.availabilityType).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = SellingPartnerApiForPricing[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
