/**
 * Selling Partner API for Pricing
 * The Selling Partner API for Pricing helps you programmatically retrieve product pricing and offer pricing information for Amazon Marketplace products.  For more information, refer to the [Product Pricing v2022-05-01 Use Case Guide](https://developer-docs.amazon.com/sp-api/docs/product-pricing-api-v2022-05-01-use-case-guide).
 *
 * The version of the OpenAPI document: 2022-05-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'pricing_v2022_05_01', 'index.js');
const SellingPartnerApiForPricing = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new SellingPartnerApiForPricing.FeaturedOfferExpectedPriceResponseBody();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(SellingPartnerApiForPricing.FeaturedOfferExpectedPriceResponseBody.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('FeaturedOfferExpectedPriceResponseBody', () => {
  it('should create an instance of FeaturedOfferExpectedPriceResponseBody', () => {
    expect(instance).to.be.a(SellingPartnerApiForPricing.FeaturedOfferExpectedPriceResponseBody);
  });

  it('should have the property offerIdentifier', () => {
    // verify property exists
    expect(instance).to.have.property('offerIdentifier');

    // set and verify value
    const expectedValue = generateMockData('OfferIdentifier');
    instance.offerIdentifier = expectedValue;
    expect(instance.offerIdentifier).to.equal(expectedValue);
  });

  it('should have the property featuredOfferExpectedPriceResults', () => {
    // verify property exists
    expect(instance).to.have.property('featuredOfferExpectedPriceResults');

    // set and verify value
    const expectedValue = generateMockData('FeaturedOfferExpectedPriceResult', true);
    instance.featuredOfferExpectedPriceResults = expectedValue;
    expect(instance.featuredOfferExpectedPriceResults).to.equal(expectedValue);
  });

  it('should have the property errors', () => {
    // verify property exists
    expect(instance).to.have.property('errors');

    // set and verify value
    const expectedValue = generateMockData('Error', true);
    instance.errors = expectedValue;
    expect(instance.errors).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = SellingPartnerApiForPricing[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
