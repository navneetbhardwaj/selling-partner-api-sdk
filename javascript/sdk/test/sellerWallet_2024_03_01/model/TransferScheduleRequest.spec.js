/**
 * The Selling Partner API for Amazon Seller Wallet Open Banking API
 * The Selling Partner API for Seller Wallet (Seller Wallet API) provides financial information that is relevant to a seller's Seller Wallet account. You can obtain financial events, balances, and transfer schedules for Seller Wallet accounts. You can also schedule and initiate transactions.
 *
 * The version of the OpenAPI document: 2024-03-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'sellerWallet_2024_03_01', 'index.js');
const TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi.TransferScheduleRequest();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi.TransferScheduleRequest.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('TransferScheduleRequest', () => {
  it('should create an instance of TransferScheduleRequest', () => {
    expect(instance).to.be.a(TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi.TransferScheduleRequest);
  });

  it('should have the property sourceAccountId', () => {
    // verify property exists
    expect(instance).to.have.property('sourceAccountId');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.sourceAccountId = expectedValue;
    expect(instance.sourceAccountId).to.equal(expectedValue);
  });

  it('should have the property sourceCurrencyCode', () => {
    // verify property exists
    expect(instance).to.have.property('sourceCurrencyCode');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.sourceCurrencyCode = expectedValue;
    expect(instance.sourceCurrencyCode).to.equal(expectedValue);
  });

  it('should have the property destinationAccountId', () => {
    // verify property exists
    expect(instance).to.have.property('destinationAccountId');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.destinationAccountId = expectedValue;
    expect(instance.destinationAccountId).to.equal(expectedValue);
  });

  it('should have the property destinationTransactionInstrument', () => {
    // verify property exists
    expect(instance).to.have.property('destinationTransactionInstrument');

    // set and verify value
    const expectedValue = generateMockData('TransactionInstrumentDetails');
    instance.destinationTransactionInstrument = expectedValue;
    expect(instance.destinationTransactionInstrument).to.equal(expectedValue);
  });

  it('should have the property transactionType', () => {
    // verify property exists
    expect(instance).to.have.property('transactionType');

    // set and verify value
    const expectedValue = generateMockData('TransactionType');
    instance.transactionType = expectedValue;
    expect(instance.transactionType).to.equal(expectedValue);
  });

  it('should have the property transferScheduleInformation', () => {
    // verify property exists
    expect(instance).to.have.property('transferScheduleInformation');

    // set and verify value
    const expectedValue = generateMockData('TransferScheduleInformation');
    instance.transferScheduleInformation = expectedValue;
    expect(instance.transferScheduleInformation).to.equal(expectedValue);
  });

  it('should have the property paymentPreference', () => {
    // verify property exists
    expect(instance).to.have.property('paymentPreference');

    // set and verify value
    const expectedValue = generateMockData('PaymentPreference');
    instance.paymentPreference = expectedValue;
    expect(instance.paymentPreference).to.equal(expectedValue);
  });

  it('should have the property transferScheduleStatus', () => {
    // verify property exists
    expect(instance).to.have.property('transferScheduleStatus');

    // set and verify value
    const expectedValue = generateMockData('TransferScheduleStatus');
    instance.transferScheduleStatus = expectedValue;
    expect(instance.transferScheduleStatus).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
