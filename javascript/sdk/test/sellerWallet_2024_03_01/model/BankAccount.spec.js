/**
 * The Selling Partner API for Amazon Seller Wallet Open Banking API
 * The Selling Partner API for Seller Wallet (Seller Wallet API) provides financial information that is relevant to a seller's Seller Wallet account. You can obtain financial events, balances, and transfer schedules for Seller Wallet accounts. You can also schedule and initiate transactions.
 *
 * The version of the OpenAPI document: 2024-03-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'sellerWallet_2024_03_01', 'index.js');
const TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi.BankAccount();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi.BankAccount.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('BankAccount', () => {
  it('should create an instance of BankAccount', () => {
    expect(instance).to.be.a(TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi.BankAccount);
  });

  it('should have the property accountId', () => {
    // verify property exists
    expect(instance).to.have.property('accountId');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.accountId = expectedValue;
    expect(instance.accountId).to.equal(expectedValue);
  });

  it('should have the property accountHolderName', () => {
    // verify property exists
    expect(instance).to.have.property('accountHolderName');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.accountHolderName = expectedValue;
    expect(instance.accountHolderName).to.equal(expectedValue);
  });

  it('should have the property bankAccountNumberFormat', () => {
    // verify property exists
    expect(instance).to.have.property('bankAccountNumberFormat');

    // set and verify value
    const expectedValue = generateMockData('BankAccountNumberFormat');
    instance.bankAccountNumberFormat = expectedValue;
    expect(instance.bankAccountNumberFormat).to.equal(expectedValue);
  });

  it('should have the property bankName', () => {
    // verify property exists
    expect(instance).to.have.property('bankName');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.bankName = expectedValue;
    expect(instance.bankName).to.equal(expectedValue);
  });

  it('should have the property bankAccountOwnershipType', () => {
    // verify property exists
    expect(instance).to.have.property('bankAccountOwnershipType');

    // set and verify value
    const expectedValue = generateMockData('BankAccountOwnershipType');
    instance.bankAccountOwnershipType = expectedValue;
    expect(instance.bankAccountOwnershipType).to.equal(expectedValue);
  });

  it('should have the property routingNumber', () => {
    // verify property exists
    expect(instance).to.have.property('routingNumber');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.routingNumber = expectedValue;
    expect(instance.routingNumber).to.equal(expectedValue);
  });

  it('should have the property bankNumberFormat', () => {
    // verify property exists
    expect(instance).to.have.property('bankNumberFormat');

    // set and verify value
    const expectedValue = generateMockData('BankNumberFormat');
    instance.bankNumberFormat = expectedValue;
    expect(instance.bankNumberFormat).to.equal(expectedValue);
  });

  it('should have the property accountCountryCode', () => {
    // verify property exists
    expect(instance).to.have.property('accountCountryCode');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.accountCountryCode = expectedValue;
    expect(instance.accountCountryCode).to.equal(expectedValue);
  });

  it('should have the property accountCurrency', () => {
    // verify property exists
    expect(instance).to.have.property('accountCurrency');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.accountCurrency = expectedValue;
    expect(instance.accountCurrency).to.equal(expectedValue);
  });

  it('should have the property bankAccountNumberTail', () => {
    // verify property exists
    expect(instance).to.have.property('bankAccountNumberTail');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.bankAccountNumberTail = expectedValue;
    expect(instance.bankAccountNumberTail).to.equal(expectedValue);
  });

  it('should have the property bankAccountHolderStatus', () => {
    // verify property exists
    expect(instance).to.have.property('bankAccountHolderStatus');

    // set and verify value
    const expectedValue = generateMockData('BankAccountHolderStatus');
    instance.bankAccountHolderStatus = expectedValue;
    expect(instance.bankAccountHolderStatus).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
