/**
 * The Selling Partner API for Amazon Seller Wallet Open Banking API
 * The Selling Partner API for Seller Wallet (Seller Wallet API) provides financial information that is relevant to a seller's Seller Wallet account. You can obtain financial events, balances, and transfer schedules for Seller Wallet accounts. You can also schedule and initiate transactions.
 *
 * The version of the OpenAPI document: 2024-03-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'sellerWallet_2024_03_01', 'index.js');
const TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi.Transaction();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi.Transaction.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('Transaction', () => {
  it('should create an instance of Transaction', () => {
    expect(instance).to.be.a(TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi.Transaction);
  });

  it('should have the property transactionId', () => {
    // verify property exists
    expect(instance).to.have.property('transactionId');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.transactionId = expectedValue;
    expect(instance.transactionId).to.equal(expectedValue);
  });

  it('should have the property transactionType', () => {
    // verify property exists
    expect(instance).to.have.property('transactionType');

    // set and verify value
    const expectedValue = generateMockData('TransactionType');
    instance.transactionType = expectedValue;
    expect(instance.transactionType).to.equal(expectedValue);
  });

  it('should have the property transactionStatus', () => {
    // verify property exists
    expect(instance).to.have.property('transactionStatus');

    // set and verify value
    const expectedValue = generateMockData('TransactionStatus');
    instance.transactionStatus = expectedValue;
    expect(instance.transactionStatus).to.equal(expectedValue);
  });

  it('should have the property transactionRequestDate', () => {
    // verify property exists
    expect(instance).to.have.property('transactionRequestDate');

    // set and verify value
    const expectedValue = generateMockData('Date');
    instance.transactionRequestDate = expectedValue;
    expect(instance.transactionRequestDate).to.equal(expectedValue);
  });

  it('should have the property expectedCompletionDate', () => {
    // verify property exists
    expect(instance).to.have.property('expectedCompletionDate');

    // set and verify value
    const expectedValue = generateMockData('Date');
    instance.expectedCompletionDate = expectedValue;
    expect(instance.expectedCompletionDate).to.equal(expectedValue);
  });

  it('should have the property transactionActualCompletionDate', () => {
    // verify property exists
    expect(instance).to.have.property('transactionActualCompletionDate');

    // set and verify value
    const expectedValue = generateMockData('Date');
    instance.transactionActualCompletionDate = expectedValue;
    expect(instance.transactionActualCompletionDate).to.equal(expectedValue);
  });

  it('should have the property lastUpdateDate', () => {
    // verify property exists
    expect(instance).to.have.property('lastUpdateDate');

    // set and verify value
    const expectedValue = generateMockData('Date');
    instance.lastUpdateDate = expectedValue;
    expect(instance.lastUpdateDate).to.equal(expectedValue);
  });

  it('should have the property requesterName', () => {
    // verify property exists
    expect(instance).to.have.property('requesterName');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.requesterName = expectedValue;
    expect(instance.requesterName).to.equal(expectedValue);
  });

  it('should have the property transactionRequesterSource', () => {
    // verify property exists
    expect(instance).to.have.property('transactionRequesterSource');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.transactionRequesterSource = expectedValue;
    expect(instance.transactionRequesterSource).to.equal(expectedValue);
  });

  it('should have the property transactionDescription', () => {
    // verify property exists
    expect(instance).to.have.property('transactionDescription');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.transactionDescription = expectedValue;
    expect(instance.transactionDescription).to.equal(expectedValue);
  });

  it('should have the property transactionSourceAccount', () => {
    // verify property exists
    expect(instance).to.have.property('transactionSourceAccount');

    // set and verify value
    const expectedValue = generateMockData('TransactionAccount');
    instance.transactionSourceAccount = expectedValue;
    expect(instance.transactionSourceAccount).to.equal(expectedValue);
  });

  it('should have the property transactionDestinationAccount', () => {
    // verify property exists
    expect(instance).to.have.property('transactionDestinationAccount');

    // set and verify value
    const expectedValue = generateMockData('TransactionAccount');
    instance.transactionDestinationAccount = expectedValue;
    expect(instance.transactionDestinationAccount).to.equal(expectedValue);
  });

  it('should have the property transactionRequestAmount', () => {
    // verify property exists
    expect(instance).to.have.property('transactionRequestAmount');

    // set and verify value
    const expectedValue = generateMockData('Currency');
    instance.transactionRequestAmount = expectedValue;
    expect(instance.transactionRequestAmount).to.equal(expectedValue);
  });

  it('should have the property transferRateDetails', () => {
    // verify property exists
    expect(instance).to.have.property('transferRateDetails');

    // set and verify value
    const expectedValue = generateMockData('TransferRatePreview');
    instance.transferRateDetails = expectedValue;
    expect(instance.transferRateDetails).to.equal(expectedValue);
  });

  it('should have the property transactionFinalAmount', () => {
    // verify property exists
    expect(instance).to.have.property('transactionFinalAmount');

    // set and verify value
    const expectedValue = generateMockData('Currency');
    instance.transactionFinalAmount = expectedValue;
    expect(instance.transactionFinalAmount).to.equal(expectedValue);
  });

  it('should have the property transactionFailureReason', () => {
    // verify property exists
    expect(instance).to.have.property('transactionFailureReason');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.transactionFailureReason = expectedValue;
    expect(instance.transactionFailureReason).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
