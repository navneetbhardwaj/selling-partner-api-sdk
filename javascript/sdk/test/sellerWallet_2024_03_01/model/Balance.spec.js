/**
 * The Selling Partner API for Amazon Seller Wallet Open Banking API
 * The Selling Partner API for Seller Wallet (Seller Wallet API) provides financial information that is relevant to a seller's Seller Wallet account. You can obtain financial events, balances, and transfer schedules for Seller Wallet accounts. You can also schedule and initiate transactions.
 *
 * The version of the OpenAPI document: 2024-03-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'sellerWallet_2024_03_01', 'index.js');
const TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi.Balance();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi.Balance.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('Balance', () => {
  it('should create an instance of Balance', () => {
    expect(instance).to.be.a(TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi.Balance);
  });

  it('should have the property accountId', () => {
    // verify property exists
    expect(instance).to.have.property('accountId');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.accountId = expectedValue;
    expect(instance.accountId).to.equal(expectedValue);
  });

  it('should have the property balanceType', () => {
    // verify property exists
    expect(instance).to.have.property('balanceType');

    // set and verify value
    const expectedValue = generateMockData('BalanceType');
    instance.balanceType = expectedValue;
    expect(instance.balanceType).to.equal(expectedValue);
  });

  it('should have the property balanceAmount', () => {
    // verify property exists
    expect(instance).to.have.property('balanceAmount');

    // set and verify value
    const expectedValue = generateMockData('Number');
    instance.balanceAmount = expectedValue;
    expect(instance.balanceAmount).to.equal(expectedValue);
  });

  it('should have the property balanceCurrency', () => {
    // verify property exists
    expect(instance).to.have.property('balanceCurrency');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.balanceCurrency = expectedValue;
    expect(instance.balanceCurrency).to.equal(expectedValue);
  });

  it('should have the property lastUpdateDate', () => {
    // verify property exists
    expect(instance).to.have.property('lastUpdateDate');

    // set and verify value
    const expectedValue = generateMockData('Date');
    instance.lastUpdateDate = expectedValue;
    expect(instance.lastUpdateDate).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = TheSellingPartnerApiForAmazonSellerWalletOpenBankingApi[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
