/**
 * The Selling Partner API for Finances
 * The Selling Partner API for Finances helps you obtain financial information relevant to a seller's business. You can obtain financial events for a given order or date range without having to wait until a statement period closes.
 *
 * The version of the OpenAPI document: 2024-06-19
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'finances_2024_06_19', 'index.js');
const TheSellingPartnerApiForFinances = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new TheSellingPartnerApiForFinances.ItemRelatedIdentifier();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(TheSellingPartnerApiForFinances.ItemRelatedIdentifier.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('ItemRelatedIdentifier', () => {
  it('should create an instance of ItemRelatedIdentifier', () => {
    expect(instance).to.be.a(TheSellingPartnerApiForFinances.ItemRelatedIdentifier);
  });

  it('should have the property itemRelatedIdentifierName', () => {
    // verify property exists
    expect(instance).to.have.property('itemRelatedIdentifierName');

    // set and verify value
    const expectedValue = ['ORDER_ADJUSTMENT_ITEM_ID', 'COUPON_ID', 'REMOVAL_SHIPMENT_ITEM_ID', 'TRANSACTION_ID', ][0];
    instance.itemRelatedIdentifierName = expectedValue;
    expect(instance.itemRelatedIdentifierName).to.equal(expectedValue);
  });

  it('should have the property itemRelatedIdentifierValue', () => {
    // verify property exists
    expect(instance).to.have.property('itemRelatedIdentifierValue');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.itemRelatedIdentifierValue = expectedValue;
    expect(instance.itemRelatedIdentifierValue).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = TheSellingPartnerApiForFinances[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
