/**
 * Selling Partner API for Replenishment
 * The Selling Partner API for Replenishment (Replenishment API) provides programmatic access to replenishment program metrics and offers. These programs provide recurring delivery of any replenishable item at a frequency chosen by the customer.  The Replenishment API is available worldwide wherever Amazon Subscribe & Save is available or is supported. The API is available to vendors and FBA selling partners.
 *
 * The version of the OpenAPI document: 2022-11-07
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import sinon from 'sinon';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'replenishment_v2022_11_07', 'index.js');
const SellingPartnerApiForReplenishment = await import(modulePath);

let instance;
let sandbox;
const testEndpoint = 'https://localhost:3000';
const testAccessToken = "testAccessToken";

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = SellingPartnerApiForReplenishment[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}

// Generate mock requests and responses for each operation
const mocklistOfferMetricsData = {
  request: {
  },
  response: {
    data: generateMockData('ListOfferMetricsResponse'),
    statusCode: 200,
    headers: {}
  }
};
const mocklistOffersData = {
  request: {
  },
  response: {
    data: generateMockData('ListOffersResponse'),
    statusCode: 200,
    headers: {}
  }
};

describe('OffersApi', () => {
  beforeEach(() => {
    sandbox = sinon.createSandbox();
    const apiClientInstance = new SellingPartnerApiForReplenishment.ApiClient(testEndpoint);
    apiClientInstance.applyXAmzAccessTokenToRequest(testAccessToken);
    sandbox.stub(apiClientInstance, 'callApi');
    instance = new SellingPartnerApiForReplenishment.OffersApi(apiClientInstance);
  });

  afterEach(() => {
    sandbox.restore();
  });

  describe('listOfferMetrics', () => {
    it('should successfully call listOfferMetrics', async () => {
      instance.apiClient.callApi.resolves(mocklistOfferMetricsData.response);

      const params = [
      ];
      const data = await instance.listOfferMetrics(...params);

      expect(data instanceof SellingPartnerApiForReplenishment.ListOfferMetricsResponse).to.be.true;
      expect(data).to.equal(mocklistOfferMetricsData.response.data);
    });

    it('should successfully call listOfferMetricsWithHttpInfo', async () => {
      instance.apiClient.callApi.resolves(mocklistOfferMetricsData.response);

      const params = [
      ];
      const response = await instance.listOfferMetricsWithHttpInfo(...params);

      expect(response).to.have.property('statusCode');
      expect(response.statusCode).to.equal(mocklistOfferMetricsData.response.statusCode)
      expect(response).to.have.property('headers');
      expect(response).to.have.property('data');
      expect(response.data).to.equal(mocklistOfferMetricsData.response.data)
    });

    it('should handle API errors', async () => {
      const errorResponse = {
        errors: new Error('Expected error to be thrown'),
        statusCode: 400,
        headers: {}
      };
      instance.apiClient.callApi.rejects(errorResponse);

      try {
        const params = [
        ];
        await instance.listOfferMetrics(...params);
        throw new Error('Expected error to be thrown');
      } catch (error) {
        expect(error).to.exist;
        expect(error.statusCode).to.equal(400);
      }
    });
  });
  describe('listOffers', () => {
    it('should successfully call listOffers', async () => {
      instance.apiClient.callApi.resolves(mocklistOffersData.response);

      const params = [
      ];
      const data = await instance.listOffers(...params);

      expect(data instanceof SellingPartnerApiForReplenishment.ListOffersResponse).to.be.true;
      expect(data).to.equal(mocklistOffersData.response.data);
    });

    it('should successfully call listOffersWithHttpInfo', async () => {
      instance.apiClient.callApi.resolves(mocklistOffersData.response);

      const params = [
      ];
      const response = await instance.listOffersWithHttpInfo(...params);

      expect(response).to.have.property('statusCode');
      expect(response.statusCode).to.equal(mocklistOffersData.response.statusCode)
      expect(response).to.have.property('headers');
      expect(response).to.have.property('data');
      expect(response.data).to.equal(mocklistOffersData.response.data)
    });

    it('should handle API errors', async () => {
      const errorResponse = {
        errors: new Error('Expected error to be thrown'),
        statusCode: 400,
        headers: {}
      };
      instance.apiClient.callApi.rejects(errorResponse);

      try {
        const params = [
        ];
        await instance.listOffers(...params);
        throw new Error('Expected error to be thrown');
      } catch (error) {
        expect(error).to.exist;
        expect(error.statusCode).to.equal(400);
      }
    });
  });

  describe('constructor', () => {
    it('should use default ApiClient when none provided', () => {
      const defaultInstance = new SellingPartnerApiForReplenishment.OffersApi();
      expect(defaultInstance.apiClient).to.equal(SellingPartnerApiForReplenishment.ApiClient.instance);
    });

    it('should use provided ApiClient', () => {
      const customClient = new SellingPartnerApiForReplenishment.ApiClient();
      const customInstance = new SellingPartnerApiForReplenishment.OffersApi(customClient);
      expect(customInstance.apiClient).to.equal(customClient);
    });
  });
});
