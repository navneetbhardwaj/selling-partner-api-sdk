/**
 * Selling Partner API for Replenishment
 * The Selling Partner API for Replenishment (Replenishment API) provides programmatic access to replenishment program metrics and offers. These programs provide recurring delivery of any replenishable item at a frequency chosen by the customer.  The Replenishment API is available worldwide wherever Amazon Subscribe & Save is available or is supported. The API is available to vendors and FBA selling partners.
 *
 * The version of the OpenAPI document: 2022-11-07
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'replenishment_v2022_11_07', 'index.js');
const SellingPartnerApiForReplenishment = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new SellingPartnerApiForReplenishment.ListOffersResponseOffer();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(SellingPartnerApiForReplenishment.ListOffersResponseOffer.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('ListOffersResponseOffer', () => {
  it('should create an instance of ListOffersResponseOffer', () => {
    expect(instance).to.be.a(SellingPartnerApiForReplenishment.ListOffersResponseOffer);
  });

  it('should have the property sku', () => {
    // verify property exists
    expect(instance).to.have.property('sku');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.sku = expectedValue;
    expect(instance.sku).to.equal(expectedValue);
  });

  it('should have the property asin', () => {
    // verify property exists
    expect(instance).to.have.property('asin');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.asin = expectedValue;
    expect(instance.asin).to.equal(expectedValue);
  });

  it('should have the property marketplaceId', () => {
    // verify property exists
    expect(instance).to.have.property('marketplaceId');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.marketplaceId = expectedValue;
    expect(instance.marketplaceId).to.equal(expectedValue);
  });

  it('should have the property eligibility', () => {
    // verify property exists
    expect(instance).to.have.property('eligibility');

    // set and verify value
    const expectedValue = generateMockData('EligibilityStatus');
    instance.eligibility = expectedValue;
    expect(instance.eligibility).to.equal(expectedValue);
  });

  it('should have the property offerProgramConfiguration', () => {
    // verify property exists
    expect(instance).to.have.property('offerProgramConfiguration');

    // set and verify value
    const expectedValue = generateMockData('OfferProgramConfiguration');
    instance.offerProgramConfiguration = expectedValue;
    expect(instance.offerProgramConfiguration).to.equal(expectedValue);
  });

  it('should have the property programType', () => {
    // verify property exists
    expect(instance).to.have.property('programType');

    // set and verify value
    const expectedValue = generateMockData('ProgramType');
    instance.programType = expectedValue;
    expect(instance.programType).to.equal(expectedValue);
  });

  it('should have the property vendorCodes', () => {
    // verify property exists
    expect(instance).to.have.property('vendorCodes');

    // set and verify value
    const expectedValue = generateMockData('String', true);
    instance.vendorCodes = expectedValue;
    expect(instance.vendorCodes).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = SellingPartnerApiForReplenishment[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
