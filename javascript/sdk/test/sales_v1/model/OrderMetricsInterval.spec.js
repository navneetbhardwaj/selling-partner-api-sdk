/**
 * Selling Partner API for Sales
 * The Selling Partner API for Sales provides APIs related to sales performance.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'sales_v1', 'index.js');
const SellingPartnerApiForSales = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new SellingPartnerApiForSales.OrderMetricsInterval();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(SellingPartnerApiForSales.OrderMetricsInterval.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('OrderMetricsInterval', () => {
  it('should create an instance of OrderMetricsInterval', () => {
    expect(instance).to.be.a(SellingPartnerApiForSales.OrderMetricsInterval);
  });

  it('should have the property interval', () => {
    // verify property exists
    expect(instance).to.have.property('interval');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.interval = expectedValue;
    expect(instance.interval).to.equal(expectedValue);
  });

  it('should have the property unitCount', () => {
    // verify property exists
    expect(instance).to.have.property('unitCount');

    // set and verify value
    const expectedValue = generateMockData('Number');
    instance.unitCount = expectedValue;
    expect(instance.unitCount).to.equal(expectedValue);
  });

  it('should have the property orderItemCount', () => {
    // verify property exists
    expect(instance).to.have.property('orderItemCount');

    // set and verify value
    const expectedValue = generateMockData('Number');
    instance.orderItemCount = expectedValue;
    expect(instance.orderItemCount).to.equal(expectedValue);
  });

  it('should have the property orderCount', () => {
    // verify property exists
    expect(instance).to.have.property('orderCount');

    // set and verify value
    const expectedValue = generateMockData('Number');
    instance.orderCount = expectedValue;
    expect(instance.orderCount).to.equal(expectedValue);
  });

  it('should have the property averageUnitPrice', () => {
    // verify property exists
    expect(instance).to.have.property('averageUnitPrice');

    // set and verify value
    const expectedValue = generateMockData('Money');
    instance.averageUnitPrice = expectedValue;
    expect(instance.averageUnitPrice).to.equal(expectedValue);
  });

  it('should have the property totalSales', () => {
    // verify property exists
    expect(instance).to.have.property('totalSales');

    // set and verify value
    const expectedValue = generateMockData('Money');
    instance.totalSales = expectedValue;
    expect(instance.totalSales).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = SellingPartnerApiForSales[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
