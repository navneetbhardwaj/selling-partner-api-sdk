/**
 * Selling Partner API for Retail Procurement Shipments
 * The Selling Partner API for Retail Procurement Shipments provides programmatic access to retail shipping data for vendors.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'vendorshipments_v1', 'index.js');
const SellingPartnerApiForRetailProcurementShipments = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new SellingPartnerApiForRetailProcurementShipments.CollectFreightPickupDetails();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(SellingPartnerApiForRetailProcurementShipments.CollectFreightPickupDetails.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('CollectFreightPickupDetails', () => {
  it('should create an instance of CollectFreightPickupDetails', () => {
    expect(instance).to.be.a(SellingPartnerApiForRetailProcurementShipments.CollectFreightPickupDetails);
  });

  it('should have the property requestedPickUp', () => {
    // verify property exists
    expect(instance).to.have.property('requestedPickUp');

    // set and verify value
    const expectedValue = generateMockData('Date');
    instance.requestedPickUp = expectedValue;
    expect(instance.requestedPickUp).to.equal(expectedValue);
  });

  it('should have the property scheduledPickUp', () => {
    // verify property exists
    expect(instance).to.have.property('scheduledPickUp');

    // set and verify value
    const expectedValue = generateMockData('Date');
    instance.scheduledPickUp = expectedValue;
    expect(instance.scheduledPickUp).to.equal(expectedValue);
  });

  it('should have the property carrierAssignmentDate', () => {
    // verify property exists
    expect(instance).to.have.property('carrierAssignmentDate');

    // set and verify value
    const expectedValue = generateMockData('Date');
    instance.carrierAssignmentDate = expectedValue;
    expect(instance.carrierAssignmentDate).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = SellingPartnerApiForRetailProcurementShipments[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
