/**
 * Selling Partner API for Reports
 * The Selling Partner API for Reports lets you retrieve and manage a variety of reports that can help selling partners manage their businesses.
 *
 * The version of the OpenAPI document: 2021-06-30
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'reports_v2021_06_30', 'index.js');
const SellingPartnerApiForReports = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new SellingPartnerApiForReports.ReportSchedule();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(SellingPartnerApiForReports.ReportSchedule.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('ReportSchedule', () => {
  it('should create an instance of ReportSchedule', () => {
    expect(instance).to.be.a(SellingPartnerApiForReports.ReportSchedule);
  });

  it('should have the property reportScheduleId', () => {
    // verify property exists
    expect(instance).to.have.property('reportScheduleId');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.reportScheduleId = expectedValue;
    expect(instance.reportScheduleId).to.equal(expectedValue);
  });

  it('should have the property reportType', () => {
    // verify property exists
    expect(instance).to.have.property('reportType');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.reportType = expectedValue;
    expect(instance.reportType).to.equal(expectedValue);
  });

  it('should have the property marketplaceIds', () => {
    // verify property exists
    expect(instance).to.have.property('marketplaceIds');

    // set and verify value
    const expectedValue = generateMockData('String', true);
    instance.marketplaceIds = expectedValue;
    expect(instance.marketplaceIds).to.equal(expectedValue);
  });

  it('should have the property reportOptions', () => {
    // verify property exists
    expect(instance).to.have.property('reportOptions');

    // set and verify value
    const expectedValue = generateMockData('{String: String}');
    instance.reportOptions = expectedValue;
    expect(instance.reportOptions).to.equal(expectedValue);
  });

  it('should have the property period', () => {
    // verify property exists
    expect(instance).to.have.property('period');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.period = expectedValue;
    expect(instance.period).to.equal(expectedValue);
  });

  it('should have the property nextReportCreationTime', () => {
    // verify property exists
    expect(instance).to.have.property('nextReportCreationTime');

    // set and verify value
    const expectedValue = generateMockData('Date');
    instance.nextReportCreationTime = expectedValue;
    expect(instance.nextReportCreationTime).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = SellingPartnerApiForReports[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
