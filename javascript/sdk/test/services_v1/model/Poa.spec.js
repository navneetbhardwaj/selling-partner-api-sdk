/**
 * Selling Partner API for Services
 * With the Services API, you can build applications that help service providers get and modify their service orders and manage their resources.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'services_v1', 'index.js');
const SellingPartnerApiForServices = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new SellingPartnerApiForServices.Poa();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(SellingPartnerApiForServices.Poa.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('Poa', () => {
  it('should create an instance of Poa', () => {
    expect(instance).to.be.a(SellingPartnerApiForServices.Poa);
  });

  it('should have the property appointmentTime', () => {
    // verify property exists
    expect(instance).to.have.property('appointmentTime');

    // set and verify value
    const expectedValue = generateMockData('AppointmentTime');
    instance.appointmentTime = expectedValue;
    expect(instance.appointmentTime).to.equal(expectedValue);
  });

  it('should have the property technicians', () => {
    // verify property exists
    expect(instance).to.have.property('technicians');

    // set and verify value
    const expectedValue = generateMockData('Technician', true);
    instance.technicians = expectedValue;
    expect(instance.technicians).to.equal(expectedValue);
  });

  it('should have the property uploadingTechnician', () => {
    // verify property exists
    expect(instance).to.have.property('uploadingTechnician');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.uploadingTechnician = expectedValue;
    expect(instance.uploadingTechnician).to.equal(expectedValue);
  });

  it('should have the property uploadTime', () => {
    // verify property exists
    expect(instance).to.have.property('uploadTime');

    // set and verify value
    const expectedValue = generateMockData('Date');
    instance.uploadTime = expectedValue;
    expect(instance.uploadTime).to.equal(expectedValue);
  });

  it('should have the property poaType', () => {
    // verify property exists
    expect(instance).to.have.property('poaType');

    // set and verify value
    const expectedValue = ['NO_SIGNATURE_DUMMY_POS', 'CUSTOMER_SIGNATURE', 'DUMMY_RECEIPT', 'POA_RECEIPT', ][0];
    instance.poaType = expectedValue;
    expect(instance.poaType).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = SellingPartnerApiForServices[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
