/**
 * The Selling Partner API for Amazon Warehousing and Distribution
 * The Selling Partner API for Amazon Warehousing and Distribution (AWD) provides programmatic access to information about AWD shipments and inventory.
 *
 * The version of the OpenAPI document: 2024-05-09
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'awd_v2024_05_09', 'index.js');
const TheSellingPartnerApiForAmazonWarehousingAndDistribution = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new TheSellingPartnerApiForAmazonWarehousingAndDistribution.SkuQuantity();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(TheSellingPartnerApiForAmazonWarehousingAndDistribution.SkuQuantity.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('SkuQuantity', () => {
  it('should create an instance of SkuQuantity', () => {
    expect(instance).to.be.a(TheSellingPartnerApiForAmazonWarehousingAndDistribution.SkuQuantity);
  });

  it('should have the property expectedQuantity', () => {
    // verify property exists
    expect(instance).to.have.property('expectedQuantity');

    // set and verify value
    const expectedValue = generateMockData('InventoryQuantity');
    instance.expectedQuantity = expectedValue;
    expect(instance.expectedQuantity).to.equal(expectedValue);
  });

  it('should have the property receivedQuantity', () => {
    // verify property exists
    expect(instance).to.have.property('receivedQuantity');

    // set and verify value
    const expectedValue = generateMockData('InventoryQuantity');
    instance.receivedQuantity = expectedValue;
    expect(instance.receivedQuantity).to.equal(expectedValue);
  });

  it('should have the property sku', () => {
    // verify property exists
    expect(instance).to.have.property('sku');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.sku = expectedValue;
    expect(instance.sku).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = TheSellingPartnerApiForAmazonWarehousingAndDistribution[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
