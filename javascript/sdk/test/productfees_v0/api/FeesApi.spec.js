/**
 * Selling Partner API for Product Fees
 * The Selling Partner API for Product Fees lets you programmatically retrieve estimated fees for a product. You can then account for those fees in your pricing.
 *
 * The version of the OpenAPI document: v0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import sinon from 'sinon';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'productfees_v0', 'index.js');
const SellingPartnerApiForProductFees = await import(modulePath);

let instance;
let sandbox;
const testEndpoint = 'https://localhost:3000';
const testAccessToken = "testAccessToken";

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = SellingPartnerApiForProductFees[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}

// Generate mock requests and responses for each operation
const mockgetMyFeesEstimateForASINData = {
  request: {
    'asin': generateMockData('String'),
    'body': generateMockData('GetMyFeesEstimateRequest')
  },
  response: {
    data: generateMockData('GetMyFeesEstimateResponse'),
    statusCode: 200,
    headers: {}
  }
};
const mockgetMyFeesEstimateForSKUData = {
  request: {
    'sellerSKU': generateMockData('String'),
    'body': generateMockData('GetMyFeesEstimateRequest')
  },
  response: {
    data: generateMockData('GetMyFeesEstimateResponse'),
    statusCode: 200,
    headers: {}
  }
};
const mockgetMyFeesEstimatesData = {
  request: {
    'body': generateMockData('[FeesEstimateByIdRequest]', true)
  },
  response: {
    data: generateMockData('FeesEstimateResult'),
    statusCode: 200,
    headers: {}
  }
};

describe('FeesApi', () => {
  beforeEach(() => {
    sandbox = sinon.createSandbox();
    const apiClientInstance = new SellingPartnerApiForProductFees.ApiClient(testEndpoint);
    apiClientInstance.applyXAmzAccessTokenToRequest(testAccessToken);
    sandbox.stub(apiClientInstance, 'callApi');
    instance = new SellingPartnerApiForProductFees.FeesApi(apiClientInstance);
  });

  afterEach(() => {
    sandbox.restore();
  });

  describe('getMyFeesEstimateForASIN', () => {
    it('should successfully call getMyFeesEstimateForASIN', async () => {
      instance.apiClient.callApi.resolves(mockgetMyFeesEstimateForASINData.response);

      const params = [
        mockgetMyFeesEstimateForASINData.request['asin'],
        mockgetMyFeesEstimateForASINData.request['body']
      ];
      const data = await instance.getMyFeesEstimateForASIN(...params);

      expect(data instanceof SellingPartnerApiForProductFees.GetMyFeesEstimateResponse).to.be.true;
      expect(data).to.equal(mockgetMyFeesEstimateForASINData.response.data);
    });

    it('should successfully call getMyFeesEstimateForASINWithHttpInfo', async () => {
      instance.apiClient.callApi.resolves(mockgetMyFeesEstimateForASINData.response);

      const params = [
        mockgetMyFeesEstimateForASINData.request['asin'],
        mockgetMyFeesEstimateForASINData.request['body']
      ];
      const response = await instance.getMyFeesEstimateForASINWithHttpInfo(...params);

      expect(response).to.have.property('statusCode');
      expect(response.statusCode).to.equal(mockgetMyFeesEstimateForASINData.response.statusCode)
      expect(response).to.have.property('headers');
      expect(response).to.have.property('data');
      expect(response.data).to.equal(mockgetMyFeesEstimateForASINData.response.data)
    });

    it('should handle API errors', async () => {
      const errorResponse = {
        errors: new Error('Expected error to be thrown'),
        statusCode: 400,
        headers: {}
      };
      instance.apiClient.callApi.rejects(errorResponse);

      try {
        const params = [
          mockgetMyFeesEstimateForASINData.request['asin'],
          mockgetMyFeesEstimateForASINData.request['body']
        ];
        await instance.getMyFeesEstimateForASIN(...params);
        throw new Error('Expected error to be thrown');
      } catch (error) {
        expect(error).to.exist;
        expect(error.statusCode).to.equal(400);
      }
    });
  });
  describe('getMyFeesEstimateForSKU', () => {
    it('should successfully call getMyFeesEstimateForSKU', async () => {
      instance.apiClient.callApi.resolves(mockgetMyFeesEstimateForSKUData.response);

      const params = [
        mockgetMyFeesEstimateForSKUData.request['sellerSKU'],
        mockgetMyFeesEstimateForSKUData.request['body']
      ];
      const data = await instance.getMyFeesEstimateForSKU(...params);

      expect(data instanceof SellingPartnerApiForProductFees.GetMyFeesEstimateResponse).to.be.true;
      expect(data).to.equal(mockgetMyFeesEstimateForSKUData.response.data);
    });

    it('should successfully call getMyFeesEstimateForSKUWithHttpInfo', async () => {
      instance.apiClient.callApi.resolves(mockgetMyFeesEstimateForSKUData.response);

      const params = [
        mockgetMyFeesEstimateForSKUData.request['sellerSKU'],
        mockgetMyFeesEstimateForSKUData.request['body']
      ];
      const response = await instance.getMyFeesEstimateForSKUWithHttpInfo(...params);

      expect(response).to.have.property('statusCode');
      expect(response.statusCode).to.equal(mockgetMyFeesEstimateForSKUData.response.statusCode)
      expect(response).to.have.property('headers');
      expect(response).to.have.property('data');
      expect(response.data).to.equal(mockgetMyFeesEstimateForSKUData.response.data)
    });

    it('should handle API errors', async () => {
      const errorResponse = {
        errors: new Error('Expected error to be thrown'),
        statusCode: 400,
        headers: {}
      };
      instance.apiClient.callApi.rejects(errorResponse);

      try {
        const params = [
          mockgetMyFeesEstimateForSKUData.request['sellerSKU'],
          mockgetMyFeesEstimateForSKUData.request['body']
        ];
        await instance.getMyFeesEstimateForSKU(...params);
        throw new Error('Expected error to be thrown');
      } catch (error) {
        expect(error).to.exist;
        expect(error.statusCode).to.equal(400);
      }
    });
  });
  describe('getMyFeesEstimates', () => {
    it('should successfully call getMyFeesEstimates', async () => {
      instance.apiClient.callApi.resolves(mockgetMyFeesEstimatesData.response);

      const params = [
        mockgetMyFeesEstimatesData.request['body']
      ];
      const data = await instance.getMyFeesEstimates(...params);

      expect(data instanceof SellingPartnerApiForProductFees.FeesEstimateResult).to.be.true;
      expect(data).to.equal(mockgetMyFeesEstimatesData.response.data);
    });

    it('should successfully call getMyFeesEstimatesWithHttpInfo', async () => {
      instance.apiClient.callApi.resolves(mockgetMyFeesEstimatesData.response);

      const params = [
        mockgetMyFeesEstimatesData.request['body']
      ];
      const response = await instance.getMyFeesEstimatesWithHttpInfo(...params);

      expect(response).to.have.property('statusCode');
      expect(response.statusCode).to.equal(mockgetMyFeesEstimatesData.response.statusCode)
      expect(response).to.have.property('headers');
      expect(response).to.have.property('data');
      expect(response.data).to.equal(mockgetMyFeesEstimatesData.response.data)
    });

    it('should handle API errors', async () => {
      const errorResponse = {
        errors: new Error('Expected error to be thrown'),
        statusCode: 400,
        headers: {}
      };
      instance.apiClient.callApi.rejects(errorResponse);

      try {
        const params = [
          mockgetMyFeesEstimatesData.request['body']
        ];
        await instance.getMyFeesEstimates(...params);
        throw new Error('Expected error to be thrown');
      } catch (error) {
        expect(error).to.exist;
        expect(error.statusCode).to.equal(400);
      }
    });
  });

  describe('constructor', () => {
    it('should use default ApiClient when none provided', () => {
      const defaultInstance = new SellingPartnerApiForProductFees.FeesApi();
      expect(defaultInstance.apiClient).to.equal(SellingPartnerApiForProductFees.ApiClient.instance);
    });

    it('should use provided ApiClient', () => {
      const customClient = new SellingPartnerApiForProductFees.ApiClient();
      const customInstance = new SellingPartnerApiForProductFees.FeesApi(customClient);
      expect(customInstance.apiClient).to.equal(customClient);
    });
  });
});
