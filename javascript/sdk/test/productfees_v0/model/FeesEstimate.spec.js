/**
 * Selling Partner API for Product Fees
 * The Selling Partner API for Product Fees lets you programmatically retrieve estimated fees for a product. You can then account for those fees in your pricing.
 *
 * The version of the OpenAPI document: v0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'productfees_v0', 'index.js');
const SellingPartnerApiForProductFees = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new SellingPartnerApiForProductFees.FeesEstimate();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(SellingPartnerApiForProductFees.FeesEstimate.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('FeesEstimate', () => {
  it('should create an instance of FeesEstimate', () => {
    expect(instance).to.be.a(SellingPartnerApiForProductFees.FeesEstimate);
  });

  it('should have the property timeOfFeesEstimation', () => {
    // verify property exists
    expect(instance).to.have.property('timeOfFeesEstimation');

    // set and verify value
    const expectedValue = generateMockData('Date');
    instance.timeOfFeesEstimation = expectedValue;
    expect(instance.timeOfFeesEstimation).to.equal(expectedValue);
  });

  it('should have the property totalFeesEstimate', () => {
    // verify property exists
    expect(instance).to.have.property('totalFeesEstimate');

    // set and verify value
    const expectedValue = generateMockData('MoneyType');
    instance.totalFeesEstimate = expectedValue;
    expect(instance.totalFeesEstimate).to.equal(expectedValue);
  });

  it('should have the property feeDetailList', () => {
    // verify property exists
    expect(instance).to.have.property('feeDetailList');

    // set and verify value
    const expectedValue = generateMockData('FeeDetail', true);
    instance.feeDetailList = expectedValue;
    expect(instance.feeDetailList).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = SellingPartnerApiForProductFees[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
