/**
 * The Selling Partner API for Automotive.
 * The Selling Partner API for Automotive provides programmatic access to information needed by selling partners to provide compatibility information about their listed products.
 *
 * The version of the OpenAPI document: 2024-11-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'vehicles_v2024_11_01', 'index.js');
const TheSellingPartnerApiForAutomotive = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new TheSellingPartnerApiForAutomotive.MonthAndYear();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(TheSellingPartnerApiForAutomotive.MonthAndYear.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('MonthAndYear', () => {
  it('should create an instance of MonthAndYear', () => {
    expect(instance).to.be.a(TheSellingPartnerApiForAutomotive.MonthAndYear);
  });

  it('should have the property year', () => {
    // verify property exists
    expect(instance).to.have.property('year');

    // set and verify value
    const expectedValue = generateMockData('Number');
    instance.year = expectedValue;
    expect(instance.year).to.equal(expectedValue);
  });

  it('should have the property month', () => {
    // verify property exists
    expect(instance).to.have.property('month');

    // set and verify value
    const expectedValue = generateMockData('Number');
    instance.month = expectedValue;
    expect(instance.month).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = TheSellingPartnerApiForAutomotive[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
