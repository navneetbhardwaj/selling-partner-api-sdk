/**
 * Selling Partner API for Direct Fulfillment Shipping
 * Use the Selling Partner API for Direct Fulfillment Shipping to access a direct fulfillment vendor's shipping data.
 *
 * The version of the OpenAPI document: 2021-12-28
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'vendordfshipping_v2021_12_28', 'index.js');
const SellingPartnerApiForDirectFulfillmentShipping = await import(modulePath);

let instance;

beforeEach(() => {
  try {
    instance = new SellingPartnerApiForDirectFulfillmentShipping.StatusUpdateDetails();
  } catch (e) {
    //Handle the cases when this model extends another model by using Model.call(this);
    instance = Object.create(SellingPartnerApiForDirectFulfillmentShipping.StatusUpdateDetails.prototype);
  }
});

afterEach(() => {
  instance = null;
});

describe('StatusUpdateDetails', () => {
  it('should create an instance of StatusUpdateDetails', () => {
    expect(instance).to.be.a(SellingPartnerApiForDirectFulfillmentShipping.StatusUpdateDetails);
  });

  it('should have the property trackingNumber', () => {
    // verify property exists
    expect(instance).to.have.property('trackingNumber');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.trackingNumber = expectedValue;
    expect(instance.trackingNumber).to.equal(expectedValue);
  });

  it('should have the property statusCode', () => {
    // verify property exists
    expect(instance).to.have.property('statusCode');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.statusCode = expectedValue;
    expect(instance.statusCode).to.equal(expectedValue);
  });

  it('should have the property reasonCode', () => {
    // verify property exists
    expect(instance).to.have.property('reasonCode');

    // set and verify value
    const expectedValue = generateMockData('String');
    instance.reasonCode = expectedValue;
    expect(instance.reasonCode).to.equal(expectedValue);
  });

  it('should have the property statusDateTime', () => {
    // verify property exists
    expect(instance).to.have.property('statusDateTime');

    // set and verify value
    const expectedValue = generateMockData('Date');
    instance.statusDateTime = expectedValue;
    expect(instance.statusDateTime).to.equal(expectedValue);
  });

  it('should have the property statusLocationAddress', () => {
    // verify property exists
    expect(instance).to.have.property('statusLocationAddress');

    // set and verify value
    const expectedValue = generateMockData('Address');
    instance.statusLocationAddress = expectedValue;
    expect(instance.statusLocationAddress).to.equal(expectedValue);
  });

  it('should have the property shipmentSchedule', () => {
    // verify property exists
    expect(instance).to.have.property('shipmentSchedule');

    // set and verify value
    const expectedValue = generateMockData('ShipmentSchedule');
    instance.shipmentSchedule = expectedValue;
    expect(instance.shipmentSchedule).to.equal(expectedValue);
  });

});

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = SellingPartnerApiForDirectFulfillmentShipping[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}
